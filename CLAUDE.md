# slides

Marp ベースのスライドを管理するレポジトリ。
Markdown ファイルを HTML スライドに変換し、GitHub Pages にデプロイする仕組みを持つ。

## スライドの追加

- 画像は `files/` ディレクトリに配置するか、Markdown 内で相対パス参照する
- `src/` 以下の任意の場所に `SLIDE.md` を作成する。`docs/template/SLIDE.md` をテンプレートとして利用する
